# Get your API key from: app.hackthebox.com > Profile > Settings > App Tokens > Create API Token
HTB_API_KEY=
REMOTE_HOST= # e.g. pi@192.168.1.x — routes all tool execution + SOCKS5 proxy through remote host
